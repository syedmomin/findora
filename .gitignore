*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build

# Locally downloaded build outputs (AAB/APK). Artifacts live in GitHub Actions.
/dist

# Signing keystore + credentials — MUST NOT be in a public repo. Kept on disk only;
# loaded into GitHub Actions Secrets instead.
keystore-backup/*.jks
keystore-backup/*.base64
keystore-backup/CREDENTIALS.txt
